React hook
=> 코드 가독성, 유지보수성 향상


useState(상태 관리) ⇢ 두 개의 요소가 담긴 배열 반환
		- const [exam, setExam] = useState(initialState);
				      ⎜		    ⎜					       ⎜
			   현재상태.  상태설정 함수			     초기값

useEffect(생명주기 관리) ⇢ 컴포넌트가 렌더링될 때마다 특정 작업을 수행할 수 있게 해줍니다.

useRef ⇢ Dom 요소나 변경해도 렌더링이 필요없는 값 저장용도
